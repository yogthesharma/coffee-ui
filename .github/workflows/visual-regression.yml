# Optional: wire Chromatic, Percy, or Playwright screenshots against `storybook-static`.
# Example (Chromatic): add repo secret CHROMATIC_PROJECT_TOKEN, then use chromaui/action
# with `workingDir: apps/playground` after `pnpm --filter @coffee-ui/playground run build-storybook`.
name: Visual regression (placeholder)

on:
  workflow_dispatch: {}

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "No visual snapshots configured."
          echo "CI already runs build-storybook; add Chromatic or Playwright as needed."
